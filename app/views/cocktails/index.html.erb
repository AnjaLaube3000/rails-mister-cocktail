<h1>Menu with all cocktails</h1>

<ul>
   <% @cocktails.each do |cocktail|  %>
    <li> <%= link_to cocktail.name, cocktail_path(cocktail)%></li>
  <% end %>
</ul>

<p> <%= link_to "Add a cocktail", new_cocktail_path(@cocktail)%></p>

